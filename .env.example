# Polymarket 自动化套利机器人配置模板
# 复制此文件为 .env 并填入实际值

# ============ 钱包配置 ============
# Polygon 钱包私钥（不含 0x 前缀）
# Private key for your Polygon wallet (without 0x prefix)
PRIVATE_KEY=your_private_key_here

# Polymarket CLOB API 配置
# Polymarket CLOB API Configuration
CLOB_API_URL=https://clob.polymarket.com
CLOB_WS_URL=wss://ws-subscriptions-clob.polymarket.com/ws/market

# ============ 交易参数 ============
# 最小套利利润率（百分比，例如 2 表示 2%）
# Minimum arbitrage profit threshold (percentage, e.g., 2 means 2%)
MIN_PROFIT_THRESHOLD=2.0

# 单笔交易最大金额（USDC）
# Maximum position size per trade (USDC)
MAX_POSITION_SIZE=100.0

# 单笔交易最小金额（USDC）
# Minimum position size per trade (USDC)
MIN_POSITION_SIZE=10.0

# 最大滑点容忍度（百分比）
# Maximum slippage tolerance (percentage)
MAX_SLIPPAGE=1.0

# 每日最大损失限额（USDC）
# Daily maximum loss limit (USDC)
DAILY_MAX_LOSS=50.0

# ============ 套利策略配置 ============
# 启用的策略（用逗号分隔）
# PRICE_IMBALANCE: 价格失衡套利 (Price imbalance arbitrage)
# CROSS_MARKET: 跨市场套利 (Cross-market arbitrage)
# TIME_BASED: 基于时间的套利 (Time-based arbitrage)
# Enabled strategies (comma-separated)
ENABLED_STRATEGIES=PRICE_IMBALANCE

# ============ 监控配置 ============
# 监控模式（选择其一）
# ALL: 监控所有市场 (Monitor all markets)
# CATEGORY: 监控指定类别 (Monitor specific categories)
# CUSTOM: 监控自定义市场列表 (Monitor custom market list)
# Monitor mode
MONITOR_MODE=CATEGORY

# 监控的类别（用逗号分隔，仅在 MONITOR_MODE=CATEGORY 时生效）
# crypto: 加密货币相关市场 (Cryptocurrency markets)
# sports: 体育赛事相关市场 (Sports events markets)
# politics: 政治事件相关市场 (Political events markets)
# entertainment: 娱乐相关市场 (Entertainment markets)
# Monitored categories (comma-separated, only effective when MONITOR_MODE=CATEGORY)
MONITOR_CATEGORIES=crypto,politics

# 自定义监控的市场 ID（用逗号分隔，仅在 MONITOR_MODE=CUSTOM 时生效）
# Custom market IDs to monitor (comma-separated, only effective when MONITOR_MODE=CUSTOM)
CUSTOM_MARKET_IDS=

# 最小市场流动性要求（USDC）
# Minimum market liquidity requirement (USDC)
MIN_LIQUIDITY=1000.0

# 最大扫描市场数量（单个监控类型）
# Maximum number of markets to scan
MAX_MARKETS=300

# 最大扫描页数
# Maximum number of pages to scan
MAX_PAGES=3

# 每页市场数量
# Number of markets per page
MARKETS_PER_PAGE=100

# ============ 风险管理 ============
# 启用风险管理
# Enable risk management
ENABLE_RISK_MANAGEMENT=true

# 最大同时持仓数量
# Maximum number of concurrent positions
MAX_CONCURRENT_POSITIONS=5

# 订单超时时间（秒）
# Order timeout (seconds)
ORDER_TIMEOUT=30

# ============ 系统配置 ============
# 日志级别（选择其一）
# debug: 调试级别 (Debug level)
# info: 信息级别 (Info level)
# warn: 警告级别 (Warning level)
# error: 错误级别 (Error level)
# Log level
LOG_LEVEL=info

# 启用实时监控面板
# Enable real-time monitoring dashboard
ENABLE_DASHBOARD=true

# WebSocket 重连间隔（毫秒）
# WebSocket reconnection interval (milliseconds)
WS_RECONNECT_INTERVAL=5000

# API 请求超时（毫秒）
# API request timeout (milliseconds)
API_TIMEOUT=10000

# API 最大重试次数
# Maximum number of API retries
API_MAX_RETRIES=4
